apiVersion: v1
kind: Pod
metadata:
  name: secret-volume
spec:
  containers:
    - name: test-webserver
      image: nginx
      volumeMounts:
        - name: secret-volume
          mountPath: /etc/secret-volume
  volumes:
    - name: secret-volume
      secret:
        secretName: test-secret
