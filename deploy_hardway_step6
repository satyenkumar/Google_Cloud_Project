KUBERNETES_PUBLIC_ADDRESS=$(gcloud compute addresses describe boonchu-cluster-1 \
  --region $(gcloud config get-value compute/region) \
  --format 'value(address)')

curl -fk --cacert ./certs/ca.pem https://${KUBERNETES_PUBLIC_ADDRESS}:6443/version
